# Local Postgres+PostGIS dev credentials for docker-compose.yml.
# Copy to infra/docker/.env (git-ignored) and set a real local password.
# This is a LOCAL DEVELOPMENT database only, never deployed with these values.
POSTGRES_DB=geostrom
POSTGRES_USER=geostrom
POSTGRES_PASSWORD=changeme_local_dev
POSTGRES_PORT=5434
